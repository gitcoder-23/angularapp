import { ProtectedfieldDirective } from './protectedfield.directive';

describe('ProtectedfieldDirective', () => {
  it('should create an instance', () => {
    const directive = new ProtectedfieldDirective();
    expect(directive).toBeTruthy();
  });
});
